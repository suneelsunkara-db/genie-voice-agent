fastapi>=0.115
uvicorn[standard]>=0.30
databricks-sdk>=0.40
requests>=2.31
# Optional: only needed for the mlflow-based DatabricksServing.from_workspace path.
# The server (server.py) uses the SDK client and does not require mlflow.
mlflow>=2.20
