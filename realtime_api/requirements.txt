fastapi>=0.115
uvicorn[standard]>=0.30
databricks-sdk>=0.40
requests>=2.31
# Optional: only for the MLflow trace mirror (tracing.py). Serving is SDK-only
# (serving_factory.build_serving) — there is no mlflow-based serving path.
mlflow>=2.20
# Semantic end-of-turn detection (Silero VAD + smart-turn v3) runs on CPU with
# numpy + onnxruntime only (no torch / transformers). Models are bundled under
# realtime_api/models/.
numpy>=1.24
onnxruntime>=1.17
