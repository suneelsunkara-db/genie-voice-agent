# Databricks Apps installs this at the app source root before running app.yaml's
# command. It pulls the local backend package (which declares databricks-sdk,
# httpx, psycopg, pydantic, pyyaml, python-dotenv) plus the API server deps.
./backend
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
pydantic>=2
# Realtime Voice API (mounted at /realtime) uses requests for TTS SSE streaming.
requests>=2.31
# Semantic end-of-turn detection (Silero VAD + smart-turn v3) for the realtime
# voice API. CPU-only, numpy + onnxruntime (no torch/transformers); ONNX models
# are bundled under realtime_api/models/.
numpy>=1.24
onnxruntime>=1.17
