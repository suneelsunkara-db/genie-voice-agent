# Databricks Apps installs this at the app source root before running app.yaml's
# command. It pulls the local backend package (which declares databricks-sdk,
# httpx, psycopg, pydantic, pyyaml, python-dotenv) plus the API server deps.
./backend
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
pydantic>=2
# Realtime Voice API (mounted at /realtime) uses requests for TTS SSE streaming.
requests>=2.31
# Semantic end-of-turn detection (Silero VAD + smart-turn v3) for the realtime
# voice API. CPU-only, numpy + onnxruntime (no torch/transformers); ONNX models
# are bundled under realtime_api/models/.
numpy>=1.24
onnxruntime>=1.17
# Model Context Protocol server, hosted in-process at /realtime/mcp (see
# mcp_server/ and api/app/main.py:_mount_mcp) so remote MCP clients can reach the
# realtime voice API as tools over HTTP. Pinned <2: mcp 2.0 dropped the bundled
# FastMCP (mcp.server.fastmcp) that mcp_server/server.py builds on; 1.x still
# ships FastMCP *and* the StreamableHTTPSessionManager the mount uses.
mcp>=1.9,<2
# Stateless in-process agent navigation; persistence and AgentOS remain disabled.
langgraph>=1.2,<2
