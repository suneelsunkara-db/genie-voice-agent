# Frontend config. Copy to .env (or .env.local).
# Base URL of the backend API. No hardcoded URLs in source.
VITE_API_BASE_URL=http://localhost:8000
# Poll interval (ms) for the flow tracker.
VITE_POLL_INTERVAL_MS=2000
