# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/

# Env / secrets
.env
.env.local
config/config.local.yaml

# Local runtime
.run/

# Built frontend served by FastAPI (regenerated by deploy_app.sh)
api/app/static/*
!api/app/static/.gitkeep

# Non-runtime presentation tool - excluded from Databricks App sync
# (contains a >10MB .pptx that exceeds the Apps per-file export limit)
deck-framework/

# OS
.DS_Store
